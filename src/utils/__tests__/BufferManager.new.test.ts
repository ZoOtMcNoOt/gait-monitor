// BufferManager has been replaced by the Rust backend
// These tests are now obsolete as the functionality moved to src-tauri/src/buffer_manager.rs

describe('BufferManager (legacy)', () => {
  it('should be handled by Rust backend', () => {
    expect(true).toBe(true)
  })
})
